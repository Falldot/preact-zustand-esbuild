# Preact Zustand Esbuild

Preact + Zustand with esbuild, so fast and light!

## Use

* Install SASS

```bash
npm install -g sass
```

* Install modules

```bash
npm install
```

* Dev mode

```bash
npm run dev
```

* Release mode

```bash
npm run dist
```
